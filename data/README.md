# Data Sources

Keep static lookup data, import templates, and data dictionaries in this folder. Live project records belong in Firestore and should not be copied here.

The current region, province, RTOM, and LEA lookup is embedded in `js/dashboard.js` for compatibility with the existing app. It can be moved here later when the lookup needs to be maintained independently.
